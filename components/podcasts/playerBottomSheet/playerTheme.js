import theme from '../../../constants/theme'

const playerTheme = {
  primaryColor: theme.white,
  secondaryColor: theme.textGrey,
  button: theme.white,
  disabled: theme.darkGrayWithOpacity,
  overlay: 'rgba(0, 0, 0, 0.6)',
  background: theme.black
}

export default playerTheme
