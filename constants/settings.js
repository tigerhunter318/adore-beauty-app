const settings = {
  defaultScrollEventThrottle: 8, // ios only https://reactnative.dev/docs/scrollview#scrolleventthrottle-ios,
  viewConfigRef: {
    waitForInteraction: false,
    itemVisiblePercentThreshold: 1
  }
}

export default settings
